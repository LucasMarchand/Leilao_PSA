@model IEnumerable<Leilao.PL.Lote>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FK_Leilao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorMinimo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorMaximo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FK_Leilao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorMinimo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorMaximo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Lote">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Lote">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Lote">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
